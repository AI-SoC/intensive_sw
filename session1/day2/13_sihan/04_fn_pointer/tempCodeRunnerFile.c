for(int i=0; i<5; i++){
    //   printf("arr[%d] is %d at %p\n", i ,ap[i],&ap[i]);
    // }

    // unsigned char* abp =(unsigned char*)arr;
    // *(abp+2)= 0x5A;
    //  for(int i=0; i<20; i++){
    //   printf("mem[%d] is %2X at %p\n", i ,abp[i],&abp[i]);
    // }